# Secrets
.env
.env.*
!.env.example
certs/*.key
certs/*.pem
*.sql
*.sql.gz

# Separate fork, deployed as the upstream image
/Spoolman/

# point-of-sales has its own .gitignore for vendor/, node_modules/, public/build, storage, ...

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/

# Company stock data (generated from the internal Excel by extract_stock_rm.py)
point-of-sales/database/seeders/data/*.json
